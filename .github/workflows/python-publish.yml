# This workflow will upload a Python Package using Twine when a PR is merged.
# For more information see: https://help.github.com/en/actions/language-and-framework-guides/using-python-with-github-actions#publishing-to-package-registries

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Upload Python Package
on:
  pull_request:
    types:
      - closed

permissions:
  contents: read

jobs:
  deploy:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade -r requirements.txt
          pip install --upgrade -r requirements_test.txt
          pip install .
      - name: Run tests
        run: |
          flake8 .
          python runtests.py
      - name: Create source distribution
        run: |
          python3 -m build
      - name: Publish package
        # v1.14.2. Must stay recent enough to bundle a twine that
        # understands the Metadata-Version modern setuptools emits (2.4+),
        # otherwise the upload fails with a misleading
        # "Metadata is missing required fields: Name, Version".
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33
        with:
          user: __token__
          password: ${{ secrets.PYPI_API_TOKEN }}
